#include <stdio.h>

/**
 * main - Entry point
 *
 * Description: Calls printf and recieves value returned by printf after its
 * execution.
 * Return: Always return 0 to indicate success
 */

int main(void)
{
printf("with proper grammar, but the outcome is a piece of art,\n");
return(0);
}
